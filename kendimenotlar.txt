static variable kavramına dikkat et
her seferinde tek satır sonunda newline içerecek ama dosya sonunda newline yoksa son satır newline içermeyecek
hata ve boş argümanda NULL return
dosya okurken ve standart inputta aynı çalışacak
buffersize kavramını araştır --> bu flag hem varken hem yokken çalışacak başta default değer gerekiyor

Çözmeme gerek olmayan muhtemel sorunlar:
get_next_line() exhibits undefined behavior if the file associated with the file
descriptor is modified after the last call, while read() has not yet reached the end
of the file.
get_next_line() also exhibits undefined behavior when reading a binary file. 

Edgecase 1:
Does your function still work if the BUFFER_SIZE value is 9999? If it is 1? 10000000? Do you know why?

Satır satır okuyacak tüm dosyayı okumaya gerek yok

lseek() nedir araştır